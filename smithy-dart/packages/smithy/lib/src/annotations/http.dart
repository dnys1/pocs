class HttpHeader {
  const HttpHeader(this.key);

  final String key;
}

class HttpQuery {
  const HttpQuery(this.key);

  final String key;
}

class HttpLabel {
  const HttpLabel();
}

class HttpPayload {
  const HttpPayload();
}
